# Railway can run multiple services
# For Streamlit app (VA access):
web: streamlit run categorize_app.py --server.port $PORT --server.address 0.0.0.0

# For scraping (optional, run separately):
# scrape: python scrape_profiles.py


